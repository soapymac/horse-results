services:
  - type: web
    name: qtips-results
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn cloud_results_api:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.7
    plan: free
